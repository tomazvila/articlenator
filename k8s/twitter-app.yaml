apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twitter-articlenator-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: twitter-articlenator
spec:
  replicas: 1
  strategy:
    type: Recreate  # Required for RWO PVC - stops old pod before starting new
  selector:
    matchLabels:
      app: twitter-articlenator
  template:
    metadata:
      labels:
        app: twitter-articlenator
    spec:
      nodeSelector:
        workload: enabled
      # Uncomment for private ghcr.io repos:
      # imagePullSecrets:
      #   - name: ghcr-secret
      containers:
        - name: twitter-articlenator
          image: ghcr.io/tomazvila/articlenator:main
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
              name: http
          env:
            - name: TWITTER_ARTICLENATOR_JSON_LOGGING
              value: "true"
            - name: TWITTER_ARTICLENATOR_OUTPUT_DIR
              value: "/data/output"
            - name: TWITTER_ARTICLENATOR_CONFIG_DIR
              value: "/data/config"
            - name: TWITTER_ARTICLENATOR_REQUIRE_COOKIE_ENCRYPTION
              value: "true"
            - name: TWITTER_ARTICLENATOR_SESSION_COOKIE_SECURE
              value: "true"
            - name: TWITTER_ARTICLENATOR_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: twitter-articlenator-secrets
                  key: flask-secret-key
            - name: TWITTER_ARTICLENATOR_COOKIE_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: twitter-articlenator-secrets
                  key: youtube-cookie-encryption-key
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          volumeMounts:
            - name: data
              mountPath: /data
            - name: output
              mountPath: /data/output
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: twitter-articlenator-data
        - name: output
          hostPath:
            path: /mnt/ssdb/articlenator/output
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: twitter-articlenator
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5001
  selector:
    app: twitter-articlenator
